name: nkrv_bible
description: 개역개정 성경 앱

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # http 통신
  http: 0.13.3
  # 카카오톡 로그인
  kakao_flutter_sdk: ^0.7.1
  dio:
  json_annotation:
  package_info:
  platform:
  shared_preferences:


dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter
  test: ^1.16.3

flutter:

  uses-material-design: true
